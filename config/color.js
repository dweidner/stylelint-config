module.exports = {
  // Use long notation for hex colors, if you have to.
  'color-hex-length': 'long',
  // Disallow named colors.
  'color-named': 'never',
  // Disallow hex colors. Use hsl instead.
  'color-no-hex': true
};
