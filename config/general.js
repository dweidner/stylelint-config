module.exports = {
  // Disable indentation rules. Allows indentation of elements in BEM notation.
  'indentation': null,
  // Use consistent linebreaks.
  'linebreaks': 'unix',
  // Limit the number of adjacent empty lines.
  'max-empty-lines': 3,
  // Limit the depth of nesting.
  'max-nesting-depth': 3,
  // Use double quotes around strings (as we typically do in HTML).
  'string-quotes': 'double',
  // Disallow unknown units.
  'unit-no-unknown': true,
  // Disallow vendor prefixes for values (use autoprefixer instead).
  'value-no-vendor-prefix': true
};
