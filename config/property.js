module.exports = {
  // Disallow vendor prefixes for properties (use autoprefixer instead).
  'property-no-vendor-prefix': true,
  // Disallow redundant values in shorthand properties.
  'shorthand-property-no-redundant-values': true
};
